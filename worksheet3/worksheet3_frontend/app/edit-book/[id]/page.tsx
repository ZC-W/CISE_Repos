// enable client-side rendering for this file
"use client";

// import UpdateBookInfo component from the components folder
import UpdateBookInfo from "../../../src/components/UpdateBookInfo";

// define and export the default function component ShowBook
export default function ShowBook() {
  // Return JSX for the ShowBook component
  return <UpdateBookInfo />;
}
// defines a React functional component 'Showbook', imports the 'UpdateBookInfo' component and renders it, typically used for dynamic route where 'UpdateBookInfo' component will handle the logic for updating a book's information based on its ID.
